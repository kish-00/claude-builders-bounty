# n8n + Claude API — Automated Weekly Dev Summary

Generates a narrative weekly summary of GitHub repo activity (commits, issues, PRs)
using the Claude API, delivered via Discord or Slack.

## Setup (5 steps)

### 1. Import the Workflow

- Open your n8n instance (self-hosted or cloud)
- Go to **Workflows** → **Add Workflow** → **Import from File**
- Select `workflow.json`

### 2. Configure Credentials

Create these credentials in n8n (Settings → Credentials):

| Service | Type | What to provide |
|---------|------|-----------------|
| GitHub | OAuth2 or Personal Access Token | `repo` scope |
| Anthropic | Header Auth | `x-api-key` header with your API key |

### 3. Set Workflow Variables

Edit the **Workflow Variables** node (the first Code node after the trigger):

| Variable | Default | Description |
|----------|---------|-------------|
| `repo` | `owner/repo` | GitHub repository to summarize |
| `language` | `EN` | Output language (`EN` or `FR`) |
| `deliveryUrl` | Discord webhook URL | Where to send the summary |
| `model` | `claude-sonnet-4-20250514` | Claude model |

### 4. Activate the Workflow

Toggle **Active** in the top-right corner. The workflow runs every Friday at 17:00 UTC.

To test immediately, click **Execute Workflow** (the play button).

### 5. (Optional) Change Schedule

Edit the **Schedule Trigger** node to change day/time.

## Customization

- **Add more repos**: Duplicate the GitHub nodes and merge the results in the Code node.
- **Change output format**: Edit the **Format Output** Code node's template.
- **Email delivery**: Swap the Discord webhook node for n8n's **Email** node.

## Sample Output

```
📊 Weekly Dev Summary — owner/repo
Week of June 8 – June 14, 2026

🚀 New Features
- Added user authentication flow (3 commits)
- Implemented dark mode toggle

🐛 Bug Fixes
- Resolved login redirect loop (#127)
- Fixed date formatting in reports (#128)

🔄 Merged PRs
- #112: Refactor API routes
- #115: Add rate limiting middleware

📌 Issues Closed
- #105: "Login fails on mobile"
- #110: "Incorrect total in invoice"

Generated by Claude Sonnet 4
```
